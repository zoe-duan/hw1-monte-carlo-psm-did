# AGENTS.md

## 项目概述

本仓库用于完成课程 HW1：通过 Monte Carlo 模拟理解 PSM、DID 和 PSM-DID。项目目标不是机械运行几个估计器，而是说明不同估计方法在不同识别假设下为什么有效或失败，并用模拟结果评价一篇真实使用 PSM-DID 的应用论文。

AI 工具可以辅助代码实现、调试、写作和审查，但最终 DGP 设计、代码运行、结果解释、论文评价和提交内容由学生本人负责。

## 核心作业要求

AI agent 在协助本仓库时必须保留以下要求：

1. 至少设计三个 Monte Carlo 场景。
2. 比较 单独 PSM、DID、带协变量的 DID 和 PSM-DID。
3. 正式 Monte Carlo 每个场景至少运行 500 次。
4. 报告 平均估计值、bias、RMSE 和 标准差。
5. 生成估计量分布图。
6. 生成匹配前后协变量平衡性表。
7. 生成共同支持图。
8. 评价一篇真实使用 PSM-DID 的应用论文。
9. 在报告中说明 AI 使用方式。
10. 在 `AI_USAGE_LOG.md` 中保存完整 AI 使用记录。

## 三个模拟场景

### 场景 A：DID 表现较好

场景 A 应满足无条件平行趋势。处理选择可以依赖处理前协变量和时间不变异质性，但未处理潜在结果趋势应为共同趋势。

预期结果：

- DID 偏误应很小。
- PSM-DID 应接近 DID。
- 单独 PSM 可能因无法消除时间不变未观测异质性而有偏。

### 场景 B：PSM-DID 改善简单 DID

场景 B 应让处理选择依赖可观测处理前协变量，同时未处理潜在结果趋势也依赖这些协变量。

预期结果：

- 简单 DID 因协变量分布不平衡而有偏。
- 带协变量的 DID 和 PSM-DID 应明显改善。
- 平衡性表和共同支持图应帮助解释这种改善。

### 场景 C：PSM-DID 失败

场景 C 应展示 PSM-DID 的边界。本项目采用随时间变化的不可观测混淆：不可观测变量影响处理选择，也影响未处理潜在结果趋势。

预期结果：

- 匹配可以改善观测变量平衡。
- 带协变量的 DID 和 PSM-DID 可能部分改善。
- 由于不可观测趋势混淆仍然存在，PSM-DID 仍会有偏。

## 估计器要求

代码至少应实现以下估计器：

1. 单独 PSM：基于处理前观测变量匹配后比较处理后结果。
2. Simple DID：使用 `Delta Y_i = Y_i1 - Y_i0` 对 `treated` 回归。
3. 带协变量的 DID：使用 `Delta Y_i` 对 `treated + x1 + x2` 回归。
4. PSM-DID：先基于 `x1`、`x2` 匹配，再比较匹配个体的 `Delta Y`。

## 禁止事项

AI agent 不得执行以下操作：

1. 不得编造模拟结果。
2. 不得编造论文、DOI 或引用信息。
3. 不得删除 AI 使用说明。
4. 不得使用处理后变量进行匹配。
5. 不得使用结果变量估计倾向得分。
6. 不得在估计器中使用 `alpha` 或 `u`。
7. 不得声称 PSM-DID 可以解决所有内生性问题。
8. 不得声称匹配本身证明平行趋势。
9. 不得把 调试运行结果作为 final 报告结果。
10. 不得在不说明的情况下改变 DGP、估计对象或估计器定义。

## 变量使用规则

匹配变量默认只能使用：

```text
x1, x2
```

以下变量只能用于模拟诊断，不得用于估计或匹配：

```text
alpha, u, y, y0, y1_potential, y1_untreated, y1_treated, untreated_trend, treatment_effect, propensity_true
```

## 估计对象说明

匹配类估计器通常更自然地解释为 ATT 风格估计量。在本项目中，处理效应设为常数 `tau = 2.0`，因此 ATT 和 ATE 在数值上相同，但报告仍应说明匹配方法的解释更接近 ATT。

## Monte Carlo 规则

1. 调试运行可以使用较小重复次数，例如 `R = 20`。
2. final 运行必须使用 `R = 500`。
3. 每次 replication 必须重新生成数据。
4. bias 计算为：

```text
Bias = mean(estimate) - true treatment effect
```

5. RMSE 计算为：

```text
RMSE = sqrt(mean((estimate - true treatment effect)^2))
```

6. final 表格应保存在 `tables/`。
7. final 图形应保存在 `figures/`。

## 图形和诊断要求

必须生成：

1. 估计量分布图；
2. 共同支持图；
3. 匹配前后平衡性表；
4. 最好包含匹配前后 SMD 对比图。

图表应清楚标注场景、估计器、真实处理效应和变量含义。不得使用误导性图形。

## 报告写作规则

撰写或修改报告时必须遵守：

1. 不得编造数值结果。
2. 不得使用 调试结果。
3. 每个主要结论应对应 DGP、表格或图形。
4. 不能笼统写“PSM-DID 更好”，必须说明在哪个场景、哪个指标下更好。
5. 不能把匹配解释成平行趋势的证明。
6. 不能把 PSM-DID 解释成解决所有内生性的万能方法。
7. 对未核实的论文细节必须明确说明。

## 论文评价规则

应用论文评价必须基于真实论文，并应讨论：

1. 研究问题；
2. 处理变量；
3. 结果变量；
4. 作者为什么使用 PSM-DID；
5. 匹配变量是否为处理前变量；
6. 是否报告共同支持；
7. 是否报告平衡性检验；
8. 是否报告平行趋势或 处理前趋势 检验；
9. 是否存在随时间变化的不可观测混淆；
10. 估计对象更接近 ATT 还是 ATE；
11. 本项目 Monte Carlo 结果如何帮助评价该论文。

如果某些细节无法从原文核实，应写明“未完全核实”，不能猜测。

## AI 使用记录规则

每次 AI 对项目有实质贡献时，应更新 `AI_USAGE_LOG.md`。记录应包含：

1. 时间或阶段；
2. 使用的工具；
3. 提示词摘要；
4. AI 输出摘要；
5. 学生如何审阅、修改或采纳；
6. 修改了哪些文件；
7. 采纳状态。

AI agent 可以自动起草 AI 使用记录，但不能替学生声称“最终确认”或“完全负责”。只有学生明确确认后，才能把状态改为已接受。

## 建议工作流程

AI agent 协助时应按以下顺序工作：

1. 明确当前任务属于 DGP、估计器、Monte Carlo、诊断、绘图、论文评价、报告写作还是最终审查。
2. 说明拟修改的内容和理由。
3. 做最小必要修改，避免大规模无理由重写。
4. 保证随机种子、相对路径和输出文件可复现。
5. 如果代码变动影响结果，应提醒学生重新运行模拟并更新表图。
6. 记录 AI 使用过程。

## 最终提交前自查

提交前应确认：

- [ ] 报告包含至少三个 Monte Carlo 场景。
- [ ] final 每个场景至少 500 次重复。
- [ ] 比较了 单独 PSM、DID、带协变量的 DID 和 PSM-DID。
- [ ] 报告了 平均估计值、bias、RMSE 和 标准差。
- [ ] 包含估计量分布图。
- [ ] 包含共同支持图。
- [ ] 包含匹配前后平衡性诊断。
- [ ] 理论解释与模拟结果对应。
- [ ] 评价了一篇真实 PSM-DID 应用论文。
- [ ] 报告中包含 AI 使用说明。
- [ ] `AI_USAGE_LOG.md` 包含详细记录。
- [ ] 代码可以从干净环境复现结果。

## 最终责任声明

AI agent 只能协助实现、组织、解释和润色。最终仓库必须反映学生本人的理解。学生本人负责核实 DGP 设计、运行代码、检查估计器实现、解释结果、阅读并评价论文、修改 AI 生成文本，并确保最终提交准确且可复现。
